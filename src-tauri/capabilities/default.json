{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the control window and the Video Requests preview window",
  "windows": ["control", "vr-preview"],
  "permissions": [
    "core:default"
  ]
}
